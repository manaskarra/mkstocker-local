{
    "name": "stock-portfolio-app",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "mongodb": "^6.14.2"
    }
}
